# constraints.txt —— 客户环境复现锁（pip install -e . -c constraints.txt）
# pyproject.toml 声明兼容范围（>=x,<y）；本文件锁定一组【已验证可跑】的具体版本，
# 供客户复现与团队本地一致。升级依赖时：改范围 → 重装 → 跑全测试 → 刷新本文件。
# 生成命令：pip freeze | grep -iE '<直接依赖及其传递依赖>' | sort
PyYAML==6.0.1
anyio==4.14.1
certifi==2026.2.25
distro==1.9.0
httpx==0.28.1
idna==3.11
jiter==0.16.0
openai==2.44.0
pydantic==2.13.4
requests==2.33.1
sniffio==1.3.1
tqdm==4.68.3
unidiff==0.7.5
urllib3==2.6.3
